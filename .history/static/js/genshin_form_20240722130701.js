"use strict";

updatePlayerCount();
async function updatePlayerCount() {
	let playerCountElement = document.getElementById("playerCount");
	playerCountElement.inner
	let main = await fetch("https://pendragonscode.xyz/")HTML = users.length;
	console.log(main);
}